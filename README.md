# Увод в програмирането на C

Този архив съдържа снимки на лекционни записки и нов **Playground**, чрез който да упражняваме стъпка по стъпка основните понятия от часовете. Съдържанието по‑долу е резюме на видяното, учебен план и указания за интерактивните упражнения.

## Обзор на записките
- **Поколения езици** – машинни (ниско ниво), асемблерни (машинно зависими мнемокодове) и езици от високо ниво (машинно независими).
- **Транслация** – програмите на C се превеждат чрез компилатор или интерпретатор към машинен код (`.obj`/`.exe`).
- **Азбука на езика** – латински букви (малки/големи), цифри 0‑9 и специални символи (`+ - * / % , ; : ( ) { } [ ] # & ! < > =` и др.).
- **Лексеми** – ключови думи (`if`, `else`, `while`, `do` …), идентификатори и константи.
- **Структура на програма** – състои се от функции, като винаги има една `main()`. Блоковете се ограждат с `{ }`, операторите завършват с `;`.
- **Типове данни** – цели (`short`, `int`, `long`, със/без знак), реални (`float`, `double`), символни (`char`) и структурираните типове (ще надградим по‑късно).

## Учебен план (в синхрон с часовете)
1. **Нива на езиците и процес на компилация** – преговор на теория + малък тест.
2. **Азбука, ключови думи и идентификатори** – проверка за валидност, правила за именуване.
3. **Константи и оператори** – видове числа, аритметични и логически оператори.
4. **Структура на функция и програма** – скелет на `main`, блокове, прототипи.
5. **Типове данни и диапазони** – `signed/unsigned`, преобразувания, въвеждане и извеждане.
6. **Мини проекти** – четене на вход, пресмятания, условни оператори.

Всеки модул приключва с въпроси и практика в Playground‑а.

## Интерактивен Playground
В `/playground/playground.c` има конзолно приложение с меню от шест стъпки:
1. Обяснение за нивата и транслацията (машинни, асемблерни, високо ниво + транслятори).
2. Тренажор за азбуката и идентификаторите (описани са всички лексеми и правила, има валидатор).
3. Константи, оператори, специални символи и спазване на `;`, `{}` и запетаи.
4. Структура на функцията, прототипи, `main`, `void` функции, коментари.
5. Типове данни със сравнителна таблица и обяснение на `signed`/`unsigned`.
6. Меню с пет различни теста (по една тема за всеки модул) + обяснения защо даден отговор е правилен или грешен. Всеки резултат се записва автоматично във файл `playground/results.log`, за да проследявате прогреса си.

### Стартиране
```bash
cd /Users/doki/Documents/School/Lazo/School/UwodVProgramiraneto/C_Language
clang playground/playground.c -o playground/playground
./playground/playground
```

> Може да използвате и `gcc` вместо `clang`, ако е предпочитан във вашата среда.

### Как да тествате
Стъпките за компилация, стартиране и проверка на логовете са описани подробно в `TESTING.md`, за да имате кратък „шпаргалка“ с обяснения за всяка команда.

## Sandbox проект
- В директория `Sandbox/` има мини проект `training.c`, който съдържа един „клас“ `LessonSandbox` с последователни упражнения (if/else, switch, while, for).
- Всеки блок е богато коментиран – указва кой код да добавиш и как да го провериш.
- Стартира се с:
  ```bash
  clang Sandbox/training.c -o Sandbox/training
  ./Sandbox/training
  ```
- Подробности и идеи за разширение са описани в `Sandbox/README.md`.

## Следващи стъпки
- Минете през стъпките и изберете всички пет теста, за да проверите знанията си. За всеки грешен отговор ще получите ясно обяснение, базирано на записките.
- Когато сте уверени, пишете ми, за да добавим упражнения с условия (`if/else`) и цикли (`while`/`for`).
- Ако искате допълнителни въпроси по конкретна тема от записките, изпратете снимката и минутата от занятията, за да добавя персонализиран модул.

Успех и приятно програмиране! 😊


# Тестов наръчник

Този документ описва всяка команда, нужна за компилиране, стартиране и проследяване на резултатите от Playground‑а. Изпълнявайте стъпките последователно.

## 1. Отвори проекта
```bash
cd /Users/doki/Documents/School/Lazo/School/UwodVProgramiraneto/C_Language
```
- `cd` (change directory) казва на терминала в коя папка да работи. Тук влизаме в основната директория на курса.

## 2. Компилирай Playground‑а
```bash
clang playground/playground.c -o playground/playground
```
- `clang` е компилаторът за C. Може да го замениш с `gcc`, ако това имаш налично.
- `playground/playground.c` е изходният файл, който ще преведем.
- `-o playground/playground` казва къде да запишем готовата програма (`playground/playground`).

Ако искаш чисто изграждане, може преди това да изтриеш стария изпълним файл:
```bash
rm -f playground/playground
```
- `rm` (remove) изтрива файл, `-f` пропуска предупреждения, ако файлът липсва.

## 3. Стартирай програмата
```bash
./playground/playground
```
- `./` означава „изпълни файла от текущата папка“.
- След старта ще видиш менюто със стъпките за теория и тестове.

## 4. Премини тестовете
- Избери опция `6`, после избери конкретен тест (1–5).
- След всеки тест резултатът се записва автоматично в `playground/results.log`.

## 5. Виж историята на резултатите
```bash
cat playground/results.log
```
- `cat` (concatenate) отпечатва съдържанието на файла. Така проверяваш всички предишни опити с процентите.

## 6. Нулирай статистиката (по желание)
```bash
> playground/results.log
```
- Операторът `>` записва празен ред във файла и така на практика го изчиства. Изпълнявай тази команда само ако искаш да започнеш отчетите от нулата.

След като свършиш, изпрати ми резултатите или въпросите си и ще продължим с условия и цикли. Успех! 🚀

